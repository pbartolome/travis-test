name: CI

on: [push]

jobs:
  test:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Test workflow
      env:
        TOKEN: ${{ secrets.TOKEN }}
      run: |
        ./ngrok authtoken "$TOKEN"
        ./ngrok tcp 5900
